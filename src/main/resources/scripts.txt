CREATE TABLE board (
  name VARCHAR(255) NOT NULL,
  description TEXT,
  sid VARCHAR(255) NOT NULL PRIMARY KEY,
  owner_sid VARCHAR(255) NOT NULL,
  link VARCHAR(255)
);

CREATE TABLE users (
  name VARCHAR(255) NOT NULL,
  sid VARCHAR(255) NOT NULL PRIMARY KEY,
  email VARCHAR(100)
);
